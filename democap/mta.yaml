_schema-version: 3.3.0
ID: democap
version: 1.0.0
description: "A simple CAP project."
parameters:
  enable-parallel-deployments: true
  deploy_mode: html5-repo
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
modules:
  - name: democap-srv
    type: nodejs
    path: gen/srv
    parameters:
      instances: 1
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm-ci
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: democap-db
      - name: democap-auth
      - name: democap-destination

  - name: democap-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    requires:
      - name: democap-db

  - name: democap-app-deployer
    type: com.sap.application.content
    path: gen
    requires:
      - name: democap-auth
      - name: srv-api
      - name: democap-html5-repo-host
        parameters:
          content-target: true
          config:
            HTML5Runtime_enabled: true
    build-parameters:
      build-result: app/
      requires:
        - name: democapdemoincident
          artifacts:
            - demoincident.zip
          target-path: app/

  - name: democapdemoincident
    type: html5
    path: app/demoincident
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm ci
        - npm run build
      supported-platforms:
        []

  - name: democap-destinations
    type: com.sap.application.content
    requires:
      - name: democap-auth
        parameters:
          service-key:
            name: democap-auth-key
      - name: democap-html5-repo-host
        parameters:
          service-key:
            name: democap-html5-repo-host-key
      - name: srv-api
      - name: democap-destination
        parameters:
          content-target: true
    build-parameters:
      no-source: true
    parameters:
      content:
        instance:
          existing_destinations_policy: update
          destinations:
            - Name: democap-html5-repository
              ServiceInstanceName: democap-html5-repo-host
              ServiceKeyName: democap-html5-repo-host-key
              sap.cloud.service: democap.service
            - Name: democap-auth
              Authentication: OAuth2UserTokenExchange
              ServiceInstanceName: democap-auth
              ServiceKeyName: democap-auth-key
              sap.cloud.service: democap.service

resources:
  - name: democap-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
  - name: democap-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: democap-${org}-${space}
        tenant-mode: dedicated
        role-collections:
          - name: 'support (democap ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.support'
          - name: 'admin (democap ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.admin'
  - name: democap-destination
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-plan: lite
      config:
        HTML5Runtime_enabled: true
        init_data:
          instance:
            existing_destinations_policy: update
            destinations:
              - Name: srv-api
                URL: ~{srv-api/srv-url}
                Authentication: NoAuthentication
                Type: HTTP
                ProxyType: Internet
                HTML5.ForwardAuthToken: true
                HTML5.DynamicDestination: true
    requires:
      - name: srv-api
        group: destinations
        properties:
          name: srv-api # must be used in xs-app.json as well
          url: ~{srv-url}
          forwardAuthToken: true

  - name: democap-html5-repo-host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-host
  - name: democap-html5-runtime
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-runtime
